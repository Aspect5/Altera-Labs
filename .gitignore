# General purpose ignores
.DS_Store
*.log
*.swo
*.swp

# IDE and editor specific files
.vscode/
.idea/

# Python / Backend related ignores
__pycache__/
*.pyc
.env
venv/
.venv/
env/
instance/

# Python packaging artifacts
/backend/build/
/backend/dist/
*.egg-info/

# Test artifacts
.pytest_cache/
.coverage

# Node.js / Frontend related ignores
node_modules/
/frontend/dist/
/frontend/.eslintcache
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*


## --- Lean / Lake related ignores --- ###

# The 'build' directory contains all compiled Lean files (.olean, .c, etc.).
# These are generated by running 'lake build' and should not be versioned.
/backend/lean_verifier/build/

# The '.lake' directory is where Lake manages dependencies and its own build artifacts.
# It includes downloaded libraries (like mathlib) and binaries.
# This should never be committed to Git.
/backend/lean_verifier/.lake/

# This file is automatically generated by elan to specify the toolchain version.
# It is good practice to commit this file to ensure all developers use the same Lean version.
# Therefore, we explicitly DO NOT ignore it.
!lean-toolchain

# Optional: Ignore specific test files if you don't want them in the repo.
# /backend/lean_verifier/Test.lean

# Python virtual environments
.venv/